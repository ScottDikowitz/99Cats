<h1>
<table>
  <tr>
    <td>Birth date:</td>
    <td><%=@cat.birth_date%></td>
  </tr>
  <tr>
    <td>Age:</td>
    <td><%=@cat.age.to_i%></td>
  </tr>
  <tr>
    <td>Color:</td>
    <td><%=@cat.color%></td>
  </tr>
  <tr>
    <td>Name:</td>
    <td><%=@cat.name%></td>
  </tr>
  <tr>
    <td>Sex:</td>
    <td><%=@cat.sex%></td>
  </tr>
  <tr>
    <td>Description:</td>
    <td><%=@cat.description%></td>
  </tr>
  <% attributes = [@cat.description,@cat.sex,@cat.name,@cat.color,@cat.age.to_i,@cat.birth_date] %>
  <% descriptions = [@cat.description,@cat.sex,@cat.name,@cat.color,@cat.age.to_i,@cat.birth_date] %>

</table>
</h1>
